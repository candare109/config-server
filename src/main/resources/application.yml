spring:
  application:
    name: logistics
  profiles:
    active: dev
  devtools:
    add-properties: false
  jackson:
    serialization:
      write-date-keys-as-timestamps: false
  jpa:
    properties:
      hibernate:
       jdbc:
         time_zone: UTC

server:
  port: 7000